apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: bminfo
spec:
  selector:
    app.kubernetes.io/name: postgres
  ports:
  - name: p5432-tcp
    port: 5432
    targetPort: 5432
    protocol: TCP
